while True:
    name = input("Enter a name (or 'stop' to finish): ")
    if name.lower() == "stop":
        break

    if len(name) <= 5:
        print(name, "is a short name.")
    else:
        print(name, "is a long name.")
